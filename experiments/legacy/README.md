"""Legacy/heuristic stack — quarantined pending validation."
"""PowPowPow core is physical/resource causality, not TA prediction.
These modules present untested heuristics as predictions; they may
return only after point-in-time backtests (see backtest.py)."""
